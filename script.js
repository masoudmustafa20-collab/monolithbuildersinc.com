(()=>{const steps=[...document.querySelectorAll('.plannerStep')];let current=0,project='';const show=n=>{current=Math.max(0,Math.min(n,steps.length-1));steps.forEach((s,i)=>s.classList.toggle('active',i===current));document.querySelector('#planner').scrollIntoView({behavior:'smooth',block:'start'})};document.querySelectorAll('.choiceGrid button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.choiceGrid button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');project=b.dataset.value}));document.querySelectorAll('.plannerNext').forEach(b=>b.addEventListener('click',()=>{if(current===0&&!project){alert('Please choose a project type first.');return}show(current+1)}));document.querySelectorAll('.plannerBack').forEach(b=>b.addEventListener('click',()=>show(current-1)));document.querySelector('#plannerForm').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.currentTarget),needs=[...document.querySelectorAll('.checkGrid input:checked')].map(x=>x.value).join(', ')||'Not specified';const subject=`Project request — ${project}`;const body=`Hello Monolith Builders Inc.,\n\nI would like to discuss a project.\n\nProject: ${project}\nScope: ${needs}\nZIP: ${f.get('zip')||''}\nTiming: ${f.get('timing')||''}\nDetails: ${f.get('details')||''}\n\nName: ${f.get('name')||''}\nPhone: ${f.get('phone')||''}\nEmail: ${f.get('email')||''}`;location.href=`mailto:info@monolithbuildersinc.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`});
const imgs=[...document.querySelectorAll('.galleryImage')],box=document.querySelector('#lightbox'),view=document.querySelector('.lightboxImage'),count=document.querySelector('.lightboxCount');let idx=0,touchX=0;const open=i=>{idx=i;view.src=imgs[idx].src;view.alt=imgs[idx].alt;count.textContent=`${idx+1} / ${imgs.length}`;box.classList.add('open');box.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'};const close=()=>{box.classList.remove('open');box.setAttribute('aria-hidden','true');document.body.style.overflow=''};const move=d=>open((idx+d+imgs.length)%imgs.length);imgs.forEach((im,i)=>{im.addEventListener('click',()=>open(i));im.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' ')open(i)})});document.querySelector('.lightboxClose').onclick=close;document.querySelector('.lightboxPrev').onclick=()=>move(-1);document.querySelector('.lightboxNext').onclick=()=>move(1);box.addEventListener('click',e=>{if(e.target===box)close()});document.addEventListener('keydown',e=>{if(!box.classList.contains('open'))return;if(e.key==='Escape')close();if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)});box.addEventListener('touchstart',e=>touchX=e.changedTouches[0].clientX,{passive:true});box.addEventListener('touchend',e=>{const d=e.changedTouches[0].clientX-touchX;if(Math.abs(d)>45)move(d>0?-1:1)},{passive:true});document.querySelectorAll('.filters button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.inspiration figure').forEach(f=>f.classList.toggle('hidden',b.dataset.filter!=='all'&&f.dataset.cat!==b.dataset.filter))}))})();
